```dataviewjs
// 基于文件夹聚类所有的标签。
for (let group of dv.pages("").filter(p => p.file.folder == "blog").groupBy(p => p.file.folder.split("/")[0])) {
  dv.paragraph(`## ${group.key}`);
  dv.paragraph(
    dv.pages(`"${group.key}"`).file.tags.distinct().map(t => {return `[${t}](${t})`}).array().sort().join(" | "));
}
```

## TODO
```dataviewjs
dv.table(["File","tags", "Modified Date"],
	dv.pages("").where(b=> b.type == "blog")
		.where(b=> b.status == "TODO")
		.sort(b => b.file.mtime,"desc")
		.map(b => [b.file.link,b.tags,b.file.mtime])
	)
```

## doing
```dataviewjs
dv.table(["File","tags","Modified Date"],
	dv.pages("")
		.where(b=> b.status == "doing")
		.sort(b => b.file.mtime,"desc")
		.map(b => [b.file.link,b.tags,b.file.mtime])
	)
```


## done
```dataviewjs
dv.table(["File","tags", "Modified Date"],
	dv.pages(`"web3"`)
		.where(b=> (b.status == "done" || b.status == null ))
		.sort(b => b.file.mtime,"desc")
		.map(b => [b.file.link,b.tags,b.file.mtime])
	)
```

## index
```dataviewjs 
for(let i of dv.pagePaths(`"blog"`).groupBy(p=>p.split("/")[1])){ 
	dv.paragraph(`### ${i.key}`); 
	let a = dv.pages(`"blog/${i.key}"`).length 
	dv.paragraph(`共有==${a}==篇`); 
	 dv.list( 
		dv.pages(`"blog
		"`)
		.filter(p=>p.file.folder.split("/")[1]==i.key)
		.where(b=> b.type == "index")
		.map(p=>p.file.link)
	); 
} 
```